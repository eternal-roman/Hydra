# AUDIT 2026-04-21

Branch: `antigravity--prototype-trial-vc-build`
Scope: 7-partition parallel zero-skip sweep (Operating Rule 1).

## Tally

| Partition | HIGH | MED | LOW | Notes |
|---|---:|---:|---:|---|
| p1 engine + tuner | 2 (→ 0 actionable) | 1 | 1 | Both HIGHs reclassified after verification (see below) |
| p2 agent + streams | 0 | 1 | 4 | All critical invariants pass (SPOT-only, post-only, circuit breaker, funding bps, journal chokepoint) |
| p3 AI layer | 0 | 0 | 0 | CLEAN — rm_features pure, thesis atomic, reviewer PR-draft-only |
| p4 backtest | 4 (→ 1 actionable) | 4 | 1 | 3 of 4 HIGHs reclassified after verification |
| p5 companion | 0 | 1 | 2 | LIVE_EXECUTION opt-in contract intact; post-only hardcoded |
| p6 dashboard | 1 (→ 0 actionable) | 3 | 6 | Stale-closure "HIGH" was false positive (setNow from useState is stable) |
| p7 tests | 0 | 3 | 4 | 43 harness scenarios; RSI/ATR threshold path not in harness (gap) |

Total findings reported by sub-agents: **7 HIGH, 13 MED, 18 LOW**.
After verification: **1 HIGH (reclassified MED), 13 MED, 18 LOW** genuinely actionable.

## Fixed in this commit

| Finding | File | Severity | Fix |
|---|---|---|---|
| WS reconnect has no backoff — hammers backend at fixed 3 s on sustained outage | `dashboard/src/App.jsx:3681` | MED | Exponential backoff 3→6→12→24→60 s with ±15 % jitter; counter resets on successful `onopen` |
| `btProgress`/`btResults`/`btReviews` unbounded across long sessions | `dashboard/src/App.jsx:3399,3426,3441` | MED | New `lruCapDict` helper + `MAX_BACKTEST_DICT_ENTRIES=50`; oldest experiments evicted by insertion order |
| `CompanionDrawer.submit` setTimeout has no unmount cleanup | `dashboard/src/App.jsx:688` | MED | Timeout handle tracked in `submitTimeoutRef`; `useEffect` cleanup clears on unmount |
| `_block_bootstrap_sample` docstring falsely implied non-overlapping draws | `hydra_backtest_metrics.py:266` | MED | Clarified: "non-circular" refers to wrap-around, not cross-draw disjointness |
| `import statistics` deferred inside `cvd_divergence_sigma` | `hydra_engine.py:1350` | LOW | Hoisted to module-top imports alongside `math` / `time` |
| Stale "Fall back to REST polling" comment contradicting no-REST invariant | `hydra_agent.py:3299` | LOW | Rewritten to reflect actual WS-only fallback-to-cache behavior |
| Stale module docstring in `hydra_companions.config` (pre-default-on era) | `hydra_companions/config.py:1-13` | LOW | Updated to describe current default-ON chat/proposals/nudges + LIVE_EXECUTION opt-in |

## Findings verified and reclassified (NOT fixed — were false positives)

- **p1 HIGH #1** — `PositionSizer.MIN_ORDER_SIZE` / `MIN_COST` class-level mutation. **Not a bug.** Kraken's ordermin/costmin are exchange-wide constants (SOL's ordermin is the same regardless of which pair loaded it). `hydra_agent.py` and `tests/test_pair_constants.py` intentionally access the dicts as a shared registry (4 call sites + 8 test assertions). Attempted per-instance refactor broke `test_pair_constants.py`; reverted with explanatory comment on the class.
- **p1 HIGH #2** — Unwrapped `from hydra_thesis import ThesisTracker` in `HydraEngine.__init__`. **Intentional.** A core-dep ImportError should hard-fail; wrapping it in try/except would mask breakage silently.
- **p4 HIGH "quota race" / "version hardcode" / "cancel-token race"** — Behavior matches spec (quota is per-call, not per-spawned-backtest; HYDRA_VERSION is the stamp version per I12, not a live-engine mirror; cancel-token race is benign — already returns `False` cleanly).
- **p4 HIGH "ExperimentStore.list iter race"** — False positive. `iter_experiments()` already catches `OSError` on file vanishment at lines 646 and 654.
- **p6 HIGH "stale closure in ProposalCard TTL"** — False positive. `setNow` from `useState` is guaranteed stable by React; empty deps `[]` is the correct pattern.

## MED triage — items NOT fixed (large blast radius or non-issues on verification)

- **p1 warmup asymmetry (26 vs 50 candles)** — Verified intentional. `RegimeDetector.detect` returning `RANGING` <50 forces MeanReversion strategy during warmup; `SignalGenerator` running at ≥26 candles lets MeanReversion produce cautious signals early. Changing 26→50 delays all startup/resume trading by ~24 min and shifts every backtest fixture. Defer.
- **p1 hydra_engine.py:1346 `import statistics` inside method** — Cosmetic; hoist in next passive refactor.
- **p2 `time.time()` for `registered_at`** — Sub-agent itself labeled "ACCEPTABLE"; timestamp semantics correctly match wall clock. Not a bug.
- **p4 sweep_param quota accounting** — Real but requires reshaping dispatcher + worker pool + per-value quota re-gating; spec interpretation question (is sweep one "call" or N?). Defer to dedicated pass.
- **p4 queue saturation hint** — Would add `retry_after_secs` to error shape; dashboard doesn't currently read that field, so mutual change needed. Defer.
- **p4 `_atomic_write_json` umask preservation** — Windows-primary project; POSIX-only concern. Non-issue here.
- **p5 Apex soul v1.3 voice example drift** — Soul-JSON data fix, owner is the user (per memory: `feedback_apex_verbosity.md`).
- **p7 L1–L6 offline guard** — Verified already handled: L scenarios are registered at `mode="live"` which requires `--i-understand-this-places-real-orders`; CI runs mock only and never invokes them. Non-issue.
- **p7 RSI/ATR threshold harness scenarios** — Coverage gap (legitimate); adding E8/E9 is feature work, not a fix. Defer.
- **p7 `time.sleep` in `test_backtest_server.py`** — `_wait_status` has a 10 s deadline with 0.05 s polls; race on line 139 is explicitly acknowledged in comment. Not flaky in practice. Defer.

## LOW triage — verified dispositions

**Fixed (see table above):** p1 `import statistics` hoist, p2 stale balance-fallback comment, p5 stale config docstring.

**Verified as non-issues / FALSE POSITIVE:**
- **p2 `fetch_error_streak` reset in `_populate_synthetic`** — Sub-agent's own re-analysis confirmed safe (reset only reached on success path).
- **p2 try/except with `logging.warning` on date parse** — Fail-safe-to-None is the designed contract; R10 handles staleness downstream. Not a bug.
- **p2 `FakeTickerStream` passes `paper=True`** — Test double; paper flag only toggles `start()` which the double overrides. No behavior difference.
- **p4 `max_workers` clamp uses `print`** — Goes to the visible launcher console; detached production runs via `start_hydra.bat` / `start_all.bat` still route stdout to a window. Not a log-loss risk.
- **p5 exception-logging-inline pattern** — Intentional best-effort no-block design for CBP sidecar path; consolidation would change semantics. Not a bug.
- **p6 Vite dev server port 3000** — Sub-agent flagged vs 5173 but noted it's internally consistent; the CLAUDE.md note ("assumed 5173") is the stale item. Non-issue for the dashboard; CLAUDE.md can be corrected in a doc pass.
- **p6 WS disconnect / missing-state / thesis kill-switch / WS URL override / companion visibility** — All explicitly PASS in the sub-agent report; listed only as positive confirmations.
- **p6 inline-style inconsistencies** — Cosmetic; large-blast to refactor into shared module; no behavior impact. Backlog.
- **p6 a11y ARIA gaps** — Non-blocker per scope; backlog.
- **p7 `time.sleep` in `test_backtest_server`** — `_wait_status` has 10 s deadline, line 139 race is acknowledged in code comments. Not flaky; sub-agent rated LOW as a refactor opportunity.
- **p7 `test_reviewer._fake_client` mock coupling** — Adding `assert_called()` is speculative defense; sub-agent's stated risk ("if code calls other methods") is not a current code path. Not fixing.
- **p7 `test_order_book.py` manual `__main__` runner** — Tests already class-based and pytest-compatible; converting invocation style adds no coverage. Backlog.

## Self-audit

**Phase 1** (post-fix diff sweep):
- Unused imports: none added.
- Dead code: none.
- Unhandled exceptions: `lruCapDict` is pure; `Math.pow`/`Math.random` cannot throw.
- Null/empty crashes: `reconnectAttemptsRef.current` initialised to 0; first reconnect delay = 3 s (expected).
- Deprecated APIs: none.
- Misleading errors: none.
- False-positive checks: none.
- Verification commands run (final, post-LOW fixes):
  - `python -m pytest tests/test_engine.py tests/test_pair_constants.py tests/test_backtest_metrics.py -x` → **175 passed in 5.33 s**
  - `python -c "from hydra_companions import config; print(config.is_enabled(), config.live_execution_enabled())"` → `True False` (default-ON chat, default-OFF live exec)
  - `cd dashboard && npm run build` → **built in 168 ms, 321.64 kB bundle**

**Phase 2** (re-sweep against Phase-1 diff):
- `reconnectAttemptsRef` is a `useRef` — no re-render trigger on mutation, correct idiom.
- Backoff cap of 60 s × 1.15 jitter = 69 s max wait; acceptable (previously indefinite 3 s hammer).
- `lruCapDict` uses `Object.keys` ordering; JS spec guarantees insertion order for string keys, so LRU-by-insertion is correct here.
- No new HIGH items surfaced.

Phase 2 clean. Audit complete.
